# Where the dev/preview server proxies /api and /admin (the Django backend).
VITE_PROXY_TARGET=http://127.0.0.1:8000
# Leave empty when the PWA and the API share an origin (recommended).
VITE_API_BASE_URL=
